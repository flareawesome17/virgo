{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The splash page listens for a startup failure and nothing else. Once the webview moves to the local server it is running the ordinary web app, which uses no Tauri APIs at all.",
  "windows": ["main"],
  "permissions": ["core:event:default", "core:window:default", "core:app:default"]
}
